let fs = require('fs');

module.exports = data => {

    (file, template, append, error) 

    if(data.append){
        
        fs.appendFileSync(data.file, data.template, data.error(err))

    } else {

        fs.writeFileSync(file, template, )


   }

};